import { RegistrationResponseModel } from './registration-response-model';

describe('RegistrationResponseModel', () => {
  it('should create an instance', () => {
    expect(new RegistrationResponseModel()).toBeTruthy();
  });
});
